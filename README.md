# AI Score

Mobile-first React/Vite application for a live sports signal interface.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The current fixtures, odds, signal confidence values and performance statistics are demo data. The UI is prepared for later connection to a live sports-data API.
